# 双核职场 Dual Core

> 双核职场不是在判断你是哪一类人，而是在提前验证你和谁适合一起成事。

## 项目简介

双核职场是一个面向组队、合伙与职场磨合的 `A2A for Reconnect` 产品。用户先完成 `W-MBTI` 测评，生成自己的 `DualCoreCard`；当两个人互相感兴趣后，不急着交换联系方式，而是先让双方 Agent 在后台进行一轮 `Agents2Agents` 协商。系统再输出兼容性判断、风险标签与 `CollaborationManual`，帮助用户在进入现实合作前，先完成一次低成本、高密度的协作预演。

当前 MVP 主链路已经收束为：

`W-MBTI 测评 -> 双核名片 -> 广场互选 -> A2A 赛博沙盘 -> 协作说明书 -> 双向确认后交换现实名片`

## 为什么适合这次赛题

### 1. Agents2Agents：让 Agent 先替人谈一轮

我们把 A2A 放在“重新连接”最关键的位置上。Agent 在这里不是单机助手，也不是自动回复器，而是用户的协作代理。两个 Agent 会围绕真实职场议题完成三段协商：

1. `立场确认`
2. `冲突协商`
3. `规则敲定`

它服务的不是 Agent 之间的自娱自乐，而是更本质的问题：这两个人是否适合一起把事做成。

### 2. Second Me：不是外挂接口，而是分身底座

Second Me 在项目里承担三层角色：

- 作为真人身份入口和个人 Agent 载体；
- 用 `profile signals / memory` 对低置信度人格维度做复核，减少单次问卷误差；
- 将协商结论、风险提示与里程碑写回分身记忆，形成“画像 -> 协商 -> 沉淀 -> 下一次再协作”的闭环。

这意味着项目不是在外面包一层 AI，而是在 `Second Me + OpenClaw` 的生态里，让真人分身真正进入 A2A 网络。

### 3. 知乎：提供真实讨论语境，也提供分享出口

很多 Agent 产品的问题，是运行在没有真实社会语境的真空里。双核职场希望把知乎作为这套系统的“现实摩擦面”：

- 用知乎热榜、圈子或可信内容作为沙盘题源，让 Agent 在真实问题和真实分歧中暴露协作风格；
- 用知乎来源为建议和冲突判断提供可追溯依据，避免输出沦为空泛鸡汤；
- 把 `双核名片`、`协作说明书`、`AI 圆桌摘要` 组织成适合知乎分享和讨论的内容，让项目既能帮助匹配，也能在社区里持续触发新的连接。

## 项目价值

双核职场连接了三件事：

- `Second Me` 提供真实分身；
- `Agents2Agents` 完成兼容性验证；
- `知乎` 提供真实讨论与传播土壤。

它回答的不是“Agent 能替你做什么”，而是“当 Agent 成为人的延伸后，人与人如何更早、更准、更低成本地重新连接”。
