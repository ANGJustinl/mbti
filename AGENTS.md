# AGENTS.md

本文件为 **双核职场** 仓库内所有协作 Agent 的统一上下文。当前仓库处于 `0 -> 1` 启动阶段，首要任务不是堆功能，而是先把产品主链路、结构化数据与可演示体验搭稳。

## 1. 项目目标

围绕 [SPEC.md](/home/server/A2A/mbti/SPEC.md) 中定义的主链路推进实现：

1. `W-MBTI 测评`
2. `双核名片`
3. `A2A 赛博沙盘`
4. `自动排雷`
5. `协作说明书`
6. `确认后交换现实名片`

任何新增功能、页面或数据结构，都应优先服务这条主链路。

## 2. 当前阶段判断

当前阶段为 **产品规格与底座搭建期**。

默认优先级如下：

1. 明确领域模型与流程状态
2. 搭出最小可演示链路
3. 为外部依赖保留 adapter / mock 机制
4. 再做视觉强化和玩法扩展

如果实现成本与演示价值冲突，优先保证主链路闭环。

## 3. 产品语义约束

Agent 在生成文案、设计交互或命名模块时，应遵守以下语义：

1. 不把产品写成娱乐化 MBTI 测试。
2. 不把输出写成空泛鸡汤，必须能落到协作建议。
3. 不把用户关系写成普通社交配对，重点是 `协作兼容性`。
4. 保留赛博感、知乎职场感、轻文学表达，但不能牺牲清晰度。
5. 对人格结论保持克制，优先使用“倾向”“协作风格”“风险提示”等表达。

## 4. 实现原则

1. **先结构化，后生成式**：先定义好类型、状态机、数据对象，再接 LLM 文案。
2. **先可解释，后智能化**：重要结论应可追溯到题目结果、规则或回合记录。
3. **外部依赖可降级**：知乎内容、A2A 协议、搜索接口都应提供 mock 或手工配置兜底。
4. **隐私前置**：未双向确认前，不展示真实联系方式。
5. **主链路优先**：所有新增页面、文案或数据结构都应直接服务协作验证主线。

## 5. 推荐目录约定

若后续开始搭建代码，优先采用以下结构：

```text
apps/
  web/
packages/
  domain/
  prompts/
  ui/
services/
  profile-service/
  sandbox-service/
  report-service/
data/
  wmti/
  zhihu/
docs/
```

说明：

- `packages/domain`：放领域类型、状态枚举、核心规则。
- `packages/prompts`：放 Agent prompt 模板与版本。
- `data/wmti`：放题库、计分映射、结果文案模板。
- `data/zhihu`：放精选题池、护身符内容池、来源信息。

## 6. 命名建议

统一使用以下领域名词，避免同义词漂移：

- `Dual Core` / `双核`
- `W-MBTI`
- `PersonalityProfile`
- `DualCoreCard`
- `SandboxSession`
- `ConflictFlag`
- `CollaborationManual`
- `Reconnect`

不要混用 `report/manual/contract` 指代同一对象。  
默认：

- 面向用户展示时用 `协作说明书`
- 面向数据模型时用 `CollaborationManual`

## 7. Prompt 与规则设计要求

如果后续新增 Agent prompt、规则引擎或内容生成模板，请遵守：

1. Prompt 尽量模块化，区分：
   - 人格总结
   - 沙盘辩论
   - 冲突检测
   - 报告生成
2. 关键输出优先结构化 JSON，再映射为展示文案。
3. 冲突判断必须有明确标签，例如：
   - `control_conflict`
   - `ambiguity_tolerance_gap`
   - `communication_style_mismatch`
   - `execution_rhythm_gap`
4. 生成文案必须同时包含：
   - 优势
   - 风险
   - 建议动作

## 8. 前端体验要求

1. 风格关键词：`赛博职场`、`理性锋利`、`知乎社区感`、`少量文学性`。
2. 页面必须兼顾桌面端与移动端。
3. 名片、沙盘、说明书三个页面是体验重点。
4. 视觉表达可以大胆，但信息层级必须清楚。
5. 不使用“泛紫色 AI 风”默认审美；优先建立明确、克制、有辨识度的视觉系统。

## 9. 数据与内容要求

1. 题库、题型映射、人格文案模板应版本化管理。
2. 知乎题池和高质量建议内容必须记录来源。
3. 若外部内容不可实时获取，优先保证演示内容质量，而不是强行接真接口。
4. 所有示例文案都应贴近真实职场，不要写成星座配对口吻。

## 10. 做决策时的优先顺序

当出现方案冲突时，按以下顺序决策：

1. 是否更有助于解释“这两个人能不能一起做事”
2. 是否更有助于演示完整闭环
3. 是否更容易被规则兜底，避免全黑盒
4. 是否更容易后续扩展为真实产品
5. 是否更酷

## 11. 修改规则

1. 如果功能变更影响主链路或领域模型，先更新 [SPEC.md](/home/server/A2A/mbti/SPEC.md)。
2. 如果新增目录结构、命名约定或协作规范，更新本文件。
3. 如果外部依赖状态发生变化，应在实现中保留 mock 路径，不要把仓库绑定死到单一外部服务。

## 12. 当前默认假设

在没有额外说明时，默认采用以下假设推进：

1. 第一版目标是 `可演示的 MVP`，不是生产级平台。
2. 知乎相关能力允许先以人工整理内容池模拟。
3. A2A 能力允许先本地抽象和 mock，不强制第一天接真实协议。
4. 用户更关心“协作是否合适”，而不是“人格标签是否华丽”。

## 13. 协作口径

后续 Agent 在解释项目时，统一使用这句话作为口径基线：

**双核职场不是在判断你是哪一类人，而是在提前验证你和谁适合一起成事。**
